"""wifi-sentry: a rogue access point monitor for Windows.

Passive detection only. It reads the beacon information Windows already
collects and compares it against a learned baseline. It never transmits,
deauthenticates, or captures other people's traffic.
"""

__version__ = "1.0.0"
